name: Test Workflow

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Testing pwd
        run: pwd

      - name: print folder content
        run: ls





